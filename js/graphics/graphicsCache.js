// ─────────────────────────────────────────────
// GRAPHICS CACHE — Pre-computed visual assets
// ─────────────────────────────────────────────

/**
 * Initializes the graphics cache.
 * Call this once at startup.
 */
export function initGraphicsCache() {
  // Pre-compute any reusable graphics assets here if needed
}

/**
 * Clears the graphics cache.
 * Call this when resetting the game or changing modes.
 */
export function clearCache() {
  // Clear any cached graphics assets here if needed
}